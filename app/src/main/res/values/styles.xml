<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/MBTA_Green</item>
        <item name="colorPrimaryDark">@color/MBTA_Black</item>
        <item name="colorAccent">@color/MBTA_Black</item>
    </style>

</resources>
